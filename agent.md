## 外部素材管理與檢索規範

1. **禁止以程式碼硬算生成：**
   - 除非使用者明確要求，否則**嚴禁**透過程式碼繪製複雜的 SVG、Canvas、CSS 圖像，或使用 Web Audio API 生成合成音效。
   - 勿浪費 Token 或代碼空間手動編寫或合成圖片與音訊檔案。

2. **網路檢索圖像與音效：**
   - 當專案需要遊戲/UI 素材、圖示 (Icons)、精靈圖 (Sprites) 或音效 (SFX/BGM) 時，請直接使用瀏覽器或網路搜尋功能，至外部免費資料庫尋找合適的開源素材。
   - **圖像資源推薦：** SVG Repo、Iconify、OpenGameArt、itch.io、Freepik。
   - **音效資源推薦：** FreeSound、OpenGameArt、Soniss (GDC 免費包)、Mixkit。

3. **授權確認與檔案存放：**
   - 確保下載的素材符合自由/免費使用授權（例如：CC0、公有領域、MIT、CC-BY）。
   - 將素材直接下載並儲存至專案對應的資料夾中：
     - 圖像：`./assets/images/` 或 `./public/images/`
     - 音訊：`./assets/audio/` 或 `./public/audio/`

4. **標準工作流程：**
   - 搜尋/擷取素材 -> 確認授權規範 -> 下載至指定目錄 -> 在程式碼中更新相對路徑引用。

5. **規格書同步：**
   - 開發過程中若實際功能、操作方式、素材規格或技術行為與規格書不一致，請先確認實際需求後，回頭更新 `Soju大冒險_開發規格書.md`，確保規格書與目前實作保持一致。